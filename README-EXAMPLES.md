# Examples

All examples use **@stacks/connect** and **@stacks/transactions**:

- `examples/deposit-example.ts` — build deposit payload
- `examples/withdraw-example.ts` — build withdraw payload
- `examples/connect-deposit.ts` — connect + deposit
- `examples/read-balance.ts` — read contract constants

Set env vars from `.env.example` before running.
