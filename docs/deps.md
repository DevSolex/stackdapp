# Dependencies

- **@stacks/connect** — wallet connection and user signing.
- **@stacks/transactions** — build contract call payloads (makeContractCall, createContractCallPayload, Cl.uint).

Both are in package.json. Run npm install.
