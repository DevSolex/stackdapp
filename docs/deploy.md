# Deploy stacking-pool

Deploy with Clarinet to Devnet/Testnet/Mainnet. After deploy, set STACKING_POOL_CONTRACT_ADDRESS and use @stacks/connect and @stacks/transactions in your app to call deposit and withdraw.
