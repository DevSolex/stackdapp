# Contract address

Set `STACKING_POOL_CONTRACT_ADDRESS` to your deployed stacking-pool contract. Use it with buildDepositPayload and buildWithdrawPayload from @stacks/transactions and connect from @stacks/connect.
