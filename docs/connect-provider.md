# Wallet provider and @stacks/connect

Use `connect()` from @stacks/connect to get a wallet provider. Then use payloads built with @stacks/transactions (createContractCallPayload, makeContractCall) to request contract calls from the user.
