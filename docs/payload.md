# Contract call payload

Use createContractCallPayload from @stacks/transactions to build the payload. Pass to @stacks/connect so the user signs. See src/stacking-pool-tx.ts and src/withdraw-payload.ts.
