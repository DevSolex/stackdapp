# @stacks/transactions usage in stackdapp

- `makeContractCall` — build a signed contract call (node/script).
- `createContractCallPayload` — build payload for wallet (e.g. @stacks/connect).
- `Cl.uint` — encode amount for deposit/withdraw.

See `src/stacking-pool-tx.ts` and `src/withdraw-payload.ts`.
