# Clarity arguments with @stacks/transactions

Use `Cl.uint()` for amount in deposit and withdraw. Use `Cl.principal()` for principal args if you add more contract functions. Import from `@stacks/transactions`.
