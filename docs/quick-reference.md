# Quick reference

- Deposit: `buildDepositPayload` (src/stacking-pool-tx.ts) + @stacks/connect
- Withdraw: `buildWithdrawPayload` (src/withdraw-payload.ts) + @stacks/connect
- Amounts: `parseStxAmount`, `Cl.uint` from @stacks/transactions
